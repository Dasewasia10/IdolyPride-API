{
	"version": 2,
	"output": "public",
	"builds": [
		{
			"src": "api/main.py",
			"use": "@vercel/python"
		},
		{ "src": "npm run dev", "use": "@vercel/static-build" }
	],
	"routes": [
		{ "src": "/api/(.*)", "dest": "/api/main.py" },
		{ "src": "/(.*)", "dest": "/index.html" }
	]
}
