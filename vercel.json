{
	"version": 2,
	"builds": [
		{
			"src": "gunicorn main:app --bind 0.0.0.0:$PORT",
			"use": "@vercel/python"
		},
		{ "src": "npm run build", "use": "@vercel/static-build" }
	],
	"routes": [
		{ "src": "/api/(.*)", "dest": "/api/main.py" },
		{ "src": "/(.*)", "dest": "/index.html" }
	]
}
