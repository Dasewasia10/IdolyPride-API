{
	"version": 2,
	"builds": [{ "src": "npm run dev", "use": "@vercel/static-build" }],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "gunicorn main:app",
			"use": "@vercel/python"
		},
		{ "src": "/(.*)", "dest": "/index.html" }
	]
}
